import 'models/match.dart';

class TempData {
  static int userId = 0;
  static Match match = new Match(
      id: 0,
      userId: 0,
      title: "title",
      comment: "comment",
      date: "date",
      win: 0,
      draw: 0,
      lose: 0);
}
